/*! \file
    \brief Contains functions for inputing parameters
*/

/****************************************************//**
* @brief Processes parameters which user is entering
*
* @param[out] a pointer to the coefficient **a**
* @param[out] b pointer to the coefficient **b**
* @param[out] c pointer to the coefficient **c**
*********************************************************/

void user_input( double* a, double* b, double* c);

/****************************************************//**
* @brief Checks that input parameter is a number
*
* @param[in] x pointer to the input variable
*********************************************************/

void input_test(double* x);
